# 📘 Interview Q&A – Linux Command-Line Basics

1. What does `pwd` do?
   - Displays the current working directory.

2. Difference between `ls`, `ls -l`, and `ls -la`?
   - Basic list, detailed permissions, and hidden files.

3. Why did the script initially fail?
   - Execute permission was missing.

4. What does `chmod 755` mean?
   - Full access for owner, read/execute for others.

5. Why are permissions critical in security?
   - They control access to sensitive resources.
