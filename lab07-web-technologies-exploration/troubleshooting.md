# 🛠️ Troubleshooting – Lab 07

### Issue: curl command fails
- Check internet connectivity
- Verify URL spelling

### Issue: Python script error
- Ensure `requests` is installed
- Run: pip3 install requests

### Issue: Timeout errors
- Network latency is expected
- Handled intentionally in robust_api_client.py

### Issue: API returns unexpected data
- APIs may change response structure
- Always validate JSON keys
